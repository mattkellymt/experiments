# experiments

Machine learning research implementations, architectural explorations, and neural network experiments powered by [`blks`](https://github.com/mattkellymt/blks).

## Experiments Overview

- **`covers_theorem/`**: Cover's Theorem on pattern separability.
- **`density_estimation/`**: Density estimation techniques and probability models.
- **`eos/`**: PyTorch Edge of Stability (EoS) experiments with custom models and optimizers.
- **`gan/`**: Generative Adversarial Network (GAN) architecture and training scripts.
- **`neural_sorting/`**: Differentiable sorting networks.
- **`quantization/`**: Neural network quantization experiments.
- **`transformer/`**: Custom transformer architecture implementations with `blks` optimizers (Muon + AdamW).
- **`vae/`**: Variational Autoencoder (VAE) models and training logic.

## Usage

```bash
git clone https://github.com/mattkellymt/experiments.git
cd experiments
uv sync
```
